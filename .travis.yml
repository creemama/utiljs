# https://docs.travis-ci.com/user/languages/javascript-with-nodejs/

# Do not build tags; it is redundant.
if: tag IS blank

language: node_js
node_js:
 - '10'
 - '8'

cache:
  directories:
    - $HOME/.npm
    - node_modules

before_install:
  - ./scripts/install-globals.sh

install: ./scripts/install.sh
